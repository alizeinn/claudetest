# Framer Motion Starter

Next.js + Tailwind CSS + Framer Motion scaffold with reusable animation primitives:

- `FadeIn` — scroll-triggered fade-up
- `StaggerGroup` / `StaggerItem` — choreographed entrance animations
- `HoverCard` — spring-based hover lift

## Develop

```bash
npm install
npm run dev
```
